const { TestScheduler } = require("jest");
const userProfOnly = require("./metodos/profesionalesUserProfOnly");

test("probando utilidad de Usuarios filtrados por Profesion", () => {
    expect(
        userProfOnly()
    ).toBe();
});