const { TestScheduler } = require("jest");
const success = require("./metodos/profesionalesSuccess");

test("probando utilidad de rellenar Success", () => {
    expect(
        success()
    ).toBe();
});
