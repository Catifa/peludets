const { TestScheduler } = require("jest");
const geoFindMe = require("./metodos/profesionalesGeoFindMe");

test("probando utilidad de rellenar GeoFindME", () => {
    expect(
        geoFindMe()
    ).toBe();
});
